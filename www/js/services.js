angular.module('starter.services', ['ionic','ngCordova'])

.factory('db', function( $cordovaSQLite) {
  // Might use a resource here that returns a JSON array

  // Some fake testing data
  
  
  // var    db = $cordovaSQLite.openDB({name:"chatAppDb.db",location:'default'});
  
    
  //  return {
  //   get: function(query,data){

  //     return $cordovaSQLite.execute(db,query,data);
  //   }
  //  }

  
  
});
